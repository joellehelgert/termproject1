using System;
public enum SchoolType
{
    ElementarySchool,
    SecondarySchool, // Mittelschule
    PolytechnicalSchool,
    AcademicHighSchool, // berufsbildende und höhere Schule (htl, gym usw)
    SpecialNeedsSchool,
}
